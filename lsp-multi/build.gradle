apply plugin: 'java'
apply plugin: 'spring-boot'

jar.baseName = 'java-lsp-multi'

springBoot {
    mainClass = 'com.sourcegraph.lsp.multi.Application'
}

dependencies {
    compile project(':lsp-common')
}
