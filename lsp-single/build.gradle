apply plugin: 'java'
apply plugin: 'spring-boot'

jar.baseName = 'java-lsp-single'

springBoot {
    mainClass = 'com.sourcegraph.lsp.single.Application'
}

dependencies {
    compile project(':lsp-common')
}
